{
  "name": "portf-large-gallery",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "serve": "yarn clean && parcel src/index.pug",
    "clean": "rm -rf .cache dist",
    "build": "yarn clean && parcel build --public-url ./ src/index.pug",
    "typecheck": "tsc index.ts --noEmit"
  },
  "browserslist": [
    "last 1 chrome versions"
  ],
  "devDependencies": {
    "cssnano": "^4.1.10",
    "pug": "^3.0.0",
    "tailwindcss": "^1.6.2"
  }
}